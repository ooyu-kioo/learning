# アサーションの導入

```js
if (this.discountRate) {base -= (this.discountRate * base)}
⬇︎
assert(this.discountRate >= 0);
if (this.discountRate) {base -= (this.discountRate * base)}
```

## 動機

## 手順

## e.g.

そのままなので省略