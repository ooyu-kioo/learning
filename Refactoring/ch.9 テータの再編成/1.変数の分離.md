# 変数の分離

```js
let temp = 2 * (height + width);
console.log(temp);
temp = height * width;
console.log(temp);

⬇︎

const perimeter = 2 * (height + width);
console.log(perimeter);
const area = height * width;
console.log(area);
```

同じローカル変数使い回したりしちゃダメだよーって話

## 動機


## 手順


## e.g.
