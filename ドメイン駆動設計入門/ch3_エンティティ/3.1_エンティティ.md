
## 値オブジェクトとの違い
値オブジェクトもエンティティも同じドメインモデルを実装したドメインオブジェクトだけど、
同一性の識別に違いがある

- 値obj => 属性が同じであるなら同じobjである
- エンティティ => 同じ属性でも同じobjではない(userとか同じpropでも同一ではない / unique_idによる識別)


## エンティティの性質
- 可変である
- 同じ属性でも区別される(= 同一性により区別される)


## エンティティである基準
- ライフサイクルを持ち、そこに連続性が存在するか
  - userなら利用者から作成され、いろいろ変化されて削除される